package com.mail.ARS.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.mail.ARS.model.Login;

public interface Login_repo extends JpaRepository<Login, Integer>{

}
